import 'package:flutter/material.dart';

class PropertyCategorySelection extends StatelessWidget {
  const PropertyCategorySelection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
